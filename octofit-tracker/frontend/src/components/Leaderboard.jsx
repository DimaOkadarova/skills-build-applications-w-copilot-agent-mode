import { useEffect, useState } from 'react'
import { fetchCollection } from '../api.js'
import { CollectionPage } from './Users.jsx'

export default function Leaderboard() { const [items, setItems] = useState([]); const [error, setError] = useState(''); useEffect(() => { fetchCollection('leaderboard').then(setItems).catch((reason) => setError(reason.message)) }, []); return <CollectionPage eyebrow="Leaderboard" title="Earn your place" intro="Consistency is the only competition that matters." error={error}><div className="leaderboard-list">{items.sort((a, b) => a.rank - b.rank).map((item) => <article className="rank-row" key={item._id}><span className="rank">{String(item.rank).padStart(2, '0')}</span><div><strong>{item.user?.name || item.user}</strong><small>{item.period}</small></div><b>{item.points.toLocaleString()} <small>PTS</small></b></article>)}</div></CollectionPage> }