import { useEffect, useState } from 'react'
import { fetchCollection } from '../api.js'
import { CollectionPage } from './Users.jsx'

export default function Activities() { const [items, setItems] = useState([]); const [error, setError] = useState(''); useEffect(() => { fetchCollection('activities').then(setItems).catch((reason) => setError(reason.message)) }, []); return <CollectionPage eyebrow="Activity log" title="Momentum, measured" intro="Recent sessions across your OctoFit community." error={error}><div className="table-wrap"><table><thead><tr><th>Session</th><th>Athlete</th><th>Duration</th><th>Calories</th><th>Completed</th></tr></thead><tbody>{items.map((item) => <tr key={item._id}><td><strong>{item.type}</strong></td><td>{item.user?.name || item.user}</td><td>{item.durationMinutes} min</td><td>{item.calories} kcal</td><td>{new Date(item.completedAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></CollectionPage> }